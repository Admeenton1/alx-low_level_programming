#include "main.h"

/**
 * strlen - Prints the lenght of a string
 * Return: Always 0
 */
int _strlen(char *s);
{
	char *s;
	int length;

	s = "Using strlen";
	length = _strlen(s);
	printf("%d\n", lenght);
	return (0);
}

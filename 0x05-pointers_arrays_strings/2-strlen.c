#include "main.h"

/**
 * _strlen - Prints length of a string
 * @s: The string to get its length
 * Return: The length of @s
 */

int _strlen(char *s);

{
	int len = 0;

	while (*s++)
	len++;
	return (len);
}
